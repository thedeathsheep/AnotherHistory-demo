/** Same-origin path for Vite dev/preview CORS proxy (see vite-plugins/openaiCompatProxy.ts). */
export declare const OPENAI_COMPAT_PROXY_PREFIX = "/__openai_compat_proxy";
/** Request header carrying the real OpenAI-compatible API root (https://host/.../v1). */
export declare const OPENAI_COMPAT_BASE_HEADER = "X-OpenAI-Compat-Base";
